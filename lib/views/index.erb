<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>Cookbook Sinatra</title>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.1.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="/application.css">
  </head>
  <body>

    <div class="container">
      <div class="row">
        <div class="col-sm-8 col-sm-offset-2">
          <% i = 1 %>
          <% @recipes.each do |recipe| %>
            <li>
            <% status = recipe.done? ? "checked='checked'" : "" %>
            <%= i %>. <input type="checkbox" disabled <%= status %>> - <%= recipe.name %> ( <%= recipe.rating %> / 5) preparation time: <%= recipe.preptime %> mins. <a href="/destroy?id=<%= i - 1 %>">destroy</a> <a href="/mark-as-done?id=<%= i - 1 %>">Mark as done</a>
            <% i += 1 %>
            </li>
          <% end %>
          <a href="/new"> new</a>
        </div>
      </div>
    </div>


    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js"></script>
    <script src="/application.js"></script>
  </body>
</html>
